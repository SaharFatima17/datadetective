# Minimum versions rather than exact pins.
#
# Exact pins force pip to build from source on any Python version that has no
# prebuilt wheel for that exact release - which on Python 3.13/3.14 means pandas,
# scipy and statsmodels all try to compile, and fail without Visual Studio C++
# build tools. Minimum versions let pip pick a release that ships a wheel.

fastapi>=0.115.6
uvicorn[standard]>=0.34.0
sqlalchemy>=2.0.36
alembic>=1.14.0
psycopg2-binary>=2.9.10
pydantic>=2.10.4
pydantic-settings>=2.7.0
python-dotenv>=1.0.1
python-multipart>=0.0.20

pandas>=2.2.3
numpy>=2.1.3
openpyxl>=3.1.5
pyarrow>=18.1.0
duckdb>=1.1.3

scipy>=1.14.1
statsmodels>=0.14.4
scikit-learn>=1.6.0
matplotlib>=3.10.0

pypdf>=5.1.0
python-docx>=1.1.2
python-pptx>=1.0.2
beautifulsoup4>=4.12.3
httpx>=0.28.1

# --- auth (proposal Sec.19) ---
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0,<4.1
PyJWT>=2.10.1
email-validator>=2.2.0
cryptography>=44.0.0

# Optional (proposal Sec.12): Prophet is only used for series with trend
# changes, and the model selector falls back to SARIMA when it is absent.
# Uncomment if your Python version has a wheel for it.
# prophet>=1.1.6

mcp>=1.2.0
pytest>=8.0.0